// Simplified TitleBar for web - no window controls needed
import "./TitleBar.css";

export default function TitleBar() {
	return (
		<div className="titlebar">
			<div className="titlebar-left">
				<div className="window-title">Triangulator</div>
			</div>
		</div>
	);
}
